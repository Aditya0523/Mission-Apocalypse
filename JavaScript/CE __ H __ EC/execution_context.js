// Two objects are given by th javascript engine : global and this

console.log("global", global); // node => global || browser => window
console.log("this", this); // node => empty object || browser => equal to window

// area outside any function in GLOBAL AREA